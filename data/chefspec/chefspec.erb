# Chefspec testing for <%= @recipe %> recipe
require 'spec_helper'
# Start description
describe '<%= @cookbook %>::<%= @recipe %>' do
  let(:chef_run) do
    runner = ChefSpec::Runner.new(platform: '',
                                  version: '')

    runner.converge(described_recipe)
  end
end
